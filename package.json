{
  "name": "alloy-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free, browser-native parametric mesh CAD with a WebGPU renderer.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node tools/build.mjs",
    "serve": "python3 -m http.server 8000",
    "examples": "node tools/examples.mjs"
  },
  "license": "MIT",
  "engines": {
    "node": ">=22"
  }
}
